{{extend 'layout.html'}}
{{if success:}}
    Users inserted: <br/> 
	{{=BEAUTIFY(data)}}
	<br/>
Errors:<br/>
    {{=BEAUTIFY(errors)}}

Existing:<br/>
	{{=BEAUTIFY(existUsers)}}

{{else:}} 	
	{{=FORM(INPUT(_type='file',_name='csvfile'),INPUT(_type='submit',_value='Upload'))}}
{{ pass }}
