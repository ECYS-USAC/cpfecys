{{extend 'layout.html'}}
{{if success:}}
    Inserted users: <br/> 
	{{=BEAUTIFY(data)}}
	<br/>
Users not found:<br/>
    {{=BEAUTIFY(errors)}}

Existing:<br/>
	{{=BEAUTIFY(existUsers)}}

{{else:}} 	
	{{=FORM(INPUT(_type='file',_name='csvfile'),INPUT(_type='submit',_value='Upload'))}}
{{ pass }}
